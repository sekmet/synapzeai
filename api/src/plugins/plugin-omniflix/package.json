{
  "name": "@elizaos-plugins/plugin-omniflix",
  "version": "0.25.6-alpha.1",
  "type": "module",
  "agentConfig": {
    "pluginType": "elizaos:client:1.0.0",
    "pluginParameters": {
      "apiEndpoint": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "mnemonic": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "OMNIFLIX_API_URL": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "privateKey": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "rpcEndpoint": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      }
    }
  }
}
