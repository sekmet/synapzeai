{
  "name": "@elizaos-plugins/plugin-squid-router",
  "version": "0.25.6-alpha.1",
  "type": "module",
  "agentConfig": {
    "pluginType": "elizaos:client:1.0.0",
    "pluginParameters": {
      "SQUID_API_THROTTLE_INTERVAL": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "SQUID_EVM_ADDRESS": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "SQUID_EVM_PRIVATE_KEY": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "SQUID_INTEGRATOR_ID": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "SQUID_SDK_URL": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      }
    }
  }
}
