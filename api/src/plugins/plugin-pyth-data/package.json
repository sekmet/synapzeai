{
  "name": "@elizaos-plugins/plugin-pyth-data",
  "version": "0.25.6-alpha.1",
  "type": "module",
  "agentConfig": {
    "pluginType": "elizaos:client:1.0.0",
    "pluginParameters": {
      "PYTH_LOG_LEVEL": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "PYTH_MAX_PRICE_STREAMS": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "PYTH_MAX_RETRIES": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "PYTH_NETWORK_ENV": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "PYTH_RETRY_DELAY": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      }
    }
  }
}
