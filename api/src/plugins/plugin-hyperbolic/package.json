{
  "name": "@elizaos-plugins/plugin-hyperbolic",
  "version": "0.25.6-alpha.1",
  "type": "module",
  "agentConfig": {
    "pluginType": "elizaos:client:1.0.0",
    "pluginParameters": {
      "HYPERBOLIC_API_KEY": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "HYPERBOLIC_ENV": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "HYPERBOLIC_LOG_LEVEL": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "HYPERBOLIC_MAX_RETRIES": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "HYPERBOLIC_RETRY_DELAY": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "HYPERBOLIC_TIMEOUT": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      },
      "SSH_PRIVATE_KEY_PATH": {
        "type": "string",
        "minLength": 1,
        "description": "Settings for packages plugin"
      }
    }
  }
}
